apiVersion: tekton.dev/v1beta1
kind: TaskRun
metadata:
  name: bluecompute-orders-deploy-task-run
spec:
  serviceAccountName: pipeline-account
  taskRef:
    name: bluecompute-orders-deploy-task
  params:
    - name: pathToYamlFile
      value: k8s_yamls
  resources:
    inputs:
      - name: git-source-orders
        resourceRef:
          name: git-source-orders
      - name: docker-image-orders
        resourceRef:
          name: docker-image-orders
