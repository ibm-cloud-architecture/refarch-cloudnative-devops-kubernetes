apiVersion: tekton.dev/v1beta1
kind: TaskRun
metadata:
  name: bluecompute-orders-build-task-run
spec:
  serviceAccountName: pipeline-account
  taskRef:
    name: bluecompute-orders-build-task
  params:
    - name: BUILDER_IMAGE
      value: "quay.io/buildah/stable:v1.12.0"
    - name: DOCKERFILE
      value: ./Dockerfile
    - name: CONTEXT
      value: .
    - name: TLSVERIFY
      value: "true"
  resources:
    inputs:
      - name: git-source-orders
        resourceRef:
          name: git-source-orders
      - name: docker-image-orders
        resourceRef:
          name: docker-image-orders
