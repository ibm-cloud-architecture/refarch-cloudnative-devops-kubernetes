apiVersion: tekton.dev/v1beta1
kind: TaskRun
metadata:
  name: bluecompute-web-build-task-run
spec:
  serviceAccountName: pipeline-account
  taskRef:
    name: bluecompute-web-build-task
  params:
    - name: BUILDER_IMAGE
      value: "quay.io/buildah/stable:v1.12.0"
    - name: DOCKERFILE
      value: ./Dockerfile
    - name: CONTEXT
      value: .
    - name: TLSVERIFY
      value: "true"
  resources:
    inputs:
      - name: git-source-web
        resourceRef:
          name: git-source-web
      - name: docker-image-web
        resourceRef:
          name: docker-image-web
